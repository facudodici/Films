{
  "version": 2,
  "builds": [
    {
      "src": "front/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "front/public"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "front/public/$1" }
  ]
}
